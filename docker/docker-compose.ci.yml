services:
  oracle-db:
    image: container-registry.oracle.com/database/free:23.26.1.0-lite
    environment:
      ORACLE_PWD: CiPassword123
    ports:
      - "1521:1521"
    volumes:
      - ./oracle/startup:/opt/oracle/scripts/startup
    healthcheck:
      test: ["CMD", "/opt/oracle/checkDBStatus.sh"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 90s
